/**
 * @khepri/supabase-client
 *
 * Supabase database client for Khepri.
 * Provides typed queries for athletes, check-ins, goals, and constraints.
 *
 * @example
 * ```typescript
 * import { createSupabaseClient } from '@khepri/supabase-client';
 *
 * const client = createSupabaseClient(
 *   process.env.SUPABASE_URL!,
 *   process.env.SUPABASE_ANON_KEY!
 * );
 * ```
 */

// Client and types will be exported in P1-B-02
export {};
